# 服务端 LLM 配置。复制此文件为 `.env` 并填写真实值；`.env` 已被
# gitignore。服务在启动时读取这些变量（见 server/ai/llm.ts）。
#
# 认证 token。两个变量名都接受，找到第一个非空值即用：
#   - ANTHROPIC_AUTH_TOKEN: 部分 Anthropic 兼容代理使用的别名
#     （例如国内一些提供 Messages API 的厂商）
#   - ANTHROPIC_API_KEY: 官方 Anthropic SDK 的默认变量名
# 走官方 Anthropic 就设 ANTHROPIC_API_KEY；走代理就设
# ANTHROPIC_AUTH_TOKEN（一般同时还要设 ANTHROPIC_BASE_URL）。
#
# ANTHROPIC_AUTH_TOKEN=sk-ant-...
# ANTHROPIC_API_KEY=sk-ant-...

# 覆盖默认 base URL。不设就走官方 Anthropic 端点；用代理时设上。
#
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# 覆盖默认模型。默认是 `claude-sonnet-4-6`。代理通常用
# 自己的模型名（例如 `MiniMax-M3`）。
#
# ANTHROPIC_MODEL=claude-sonnet-4-6

# Git author identity used by the vault history feature for the
# first commit on a fresh vault. Only used when the vault repo has
# no `user.name` / `user.email` set yet — an existing identity in
# the repo's local git config is always preserved. Defaults to
# `docus` / `docus@localhost` if both are unset. On a personal
# single-user vault the defaults are fine; set these if you want
# commits attributed to a real name.
#
# GIT_AUTHOR_NAME=docus
# GIT_AUTHOR_EMAIL=docus@localhost
